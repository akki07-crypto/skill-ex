services:
  - type: web
    name: skillswap-hub
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npx ts-node server/server.ts
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: NODE_ENV
        value: production
